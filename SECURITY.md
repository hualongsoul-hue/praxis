# 安全策略

## 报告漏洞

请通过仓库的私密安全报告渠道提交漏洞，不要在公开 Issue 中披露密钥、利用代码或用户数据。
报告应包含受影响版本、复现步骤、影响范围和可行的缓解建议。

## 支持范围

安全修复面向最新发布版本。项目不承诺兼容未发布的旧检查点或内部 API。

## 密钥与敏感数据

- 模型密钥仅通过 `PRAXIS_MODEL_API_KEY` 环境变量注入。
- 禁止把真实密钥写入配置、示例、日志、测试快照或检查点。
- 曾进入 Git 历史的凭据必须立即在提供方轮换；删除工作树中的文本不能使旧凭据重新安全。

## 工具执行边界

本地 Shell 不是安全沙箱。处理不可信输入的服务必须在容器或操作系统隔离环境中运行，并显式启用所需工具。
